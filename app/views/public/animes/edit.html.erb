<h3>アニメ投稿内容編集</h3>
<%= form_with model:@anime, url:anime_path(@anime.id), local:true do |f| %>
  <%= render 'public/shared/anime', f: f, anime: @anime %>
  <div>
    <%= f.submit "変更を保存" %>
  </div>
<% end %>